# 扫雷游戏设计文档

> 本文档整理自前期与 AI 的设计讨论结果，作为后续编程实现的依据。
> 你可以在本文档上直接修改自己的想法（增删需求、调整技术方案、标注"待定"），确认后的版本将作为实现基线。
>
> 状态：**草稿 / 待你确认**
> 最后更新：2026-09-17

---

## 1. 项目目标

制作一个**小体量**的扫雷游戏，核心诉求有两条：

1. **小体量**：初版不考虑过多功能，先确保能够启动、能够完整游玩。
2. **模块化设计**：结构清晰、职责分离，确保后续能够自然新增内容（难度、主题、统计等），不需要推翻重写。

初版验收标准：**能启动、能游玩、核心逻辑模块化**。

---

## 2. 范围定义

### 2.1 初版包含（MVP）

- 固定难度或少量预设难度，先做 `9 x 9 / 10 雷`
- 左键翻开格子
- 右键标记 / 取消旗帜
- 首次点击后再布雷，避免首点踩雷
- 数字提示（相邻雷数 1-8）
- 空白格自动展开（连通区域扩散）
- 胜负判定
- 重开按钮
- 基础状态栏：总雷数、剩余雷数、计时（状态栏需要预留一些位置，后续会加上更多的状态显示）

### 2.2 初版暂不做

- 自定义棋盘尺寸、雷数
- 存档（排行榜）
- 多主题
- 音效
- 复杂动画

---

## 3. 技术方案

推荐技术栈：

```text
Vite + React + TypeScript
```

选型理由：

- 体量小、启动快、结构清楚
- 扫雷的核心规则适合用**纯 TypeScript 函数**实现，React 只负责界面渲染与交互
- 逻辑与视图分离后，后续新增难度、主题、统计、单元测试都会比较顺

---

## 4. 目录结构

```text
src/
  main.tsx                # 入口
  App.tsx                 # 整体状态管理，连接 UI 与游戏逻辑
  styles.css              # 全局样式
  game/                   # 纯逻辑层（不依赖 React）
    types.ts              # 类型定义
    constants.ts          # 默认难度、基础配置
    board.ts              # 创建棋盘、布雷、计算相邻雷数
    reveal.ts             # 翻开格子、空白区域扩散
    victory.ts            # 胜负判定
  components/             # 视图层
    GameHeader.tsx        # 状态栏、剩余雷数、计时、重开按钮
    Board.tsx             # 棋盘网格容器
    Cell.tsx              # 单个格子显示
```

### 4.1 模块职责

| 模块 | 职责 |
| --- | --- |
| `game/types.ts` | 定义格子、棋盘、游戏状态、难度等类型 |
| `game/constants.ts` | 默认难度、基础配置 |
| `game/board.ts` | 创建棋盘、布雷、计算相邻雷数 |
| `game/reveal.ts` | 处理翻开格子和空白区域扩散 |
| `game/victory.ts` | 判断是否胜利 |
| `components/GameHeader.tsx` | 状态栏、剩余雷数、计时、重开按钮 |
| `components/Board.tsx` | 棋盘网格 |
| `components/Cell.tsx` | 单个格子显示 |
| `App.tsx` | 管理整体状态，连接 UI 和游戏逻辑 |

---

## 5. 核心数据模型

```ts
type GameStatus = "ready" | "playing" | "won" | "lost";

type Cell = {
  row: number;
  col: number;
  hasMine: boolean;
  adjacentMines: number;
  revealed: boolean;
  flagged: boolean;
};

type Difficulty = {
  rows: number;
  cols: number;
  mines: number;
};
```

关键约定：

- 初始状态是 `ready`
- 棋盘先创建为空（不布雷），第一次点击某个格子时才生成地雷，并**排除首次点击位置**
- （待定）是否同时排除首次点击的 8 邻域，让首点必然展开一片区域

---

## 6. 游戏流程

1. 页面启动后显示默认棋盘（`ready` 状态）
2. 玩家第一次左键点击格子
3. 系统生成地雷，保证首次点击位置不是雷
4. 翻开格子：
   - 数字格：只翻开当前格
   - 空白格：自动展开相邻空白格及其边缘数字格
5. 右键格子切换旗帜
6. 点到地雷：状态变为 `lost`，展示所有地雷
7. 所有非雷格子都被翻开：状态变为 `won`
8. 点击重开按钮：重置一局

---

## 7. UI 设计方向

初版保持简洁：

- 顶部一行状态栏：剩余雷数、计时、状态、重开按钮
- 中间是固定尺寸棋盘
- 格子使用**稳定正方形尺寸**，避免内容变化导致布局跳动
- 数字 `1-8` 用不同颜色区分
- 输赢通过状态栏和棋盘反馈表达，初版不做复杂弹窗

视觉风格建议：干净、清晰、轻量的工具感，优先保证可读性和操作反馈。

---

## 8. 后续扩展预留

当前结构后续可以比较自然地支持：

| 扩展项 | 落地方式 |
| --- | --- |
| 难度选择 | 扩展 `Difficulty` |
| 自定义尺寸 | 复用棋盘生成逻辑 |
| 最佳时间 | 新增本地存储模块 |
| 主题 | 用 CSS 变量控制颜色 |
| 动画 | 只改 `Cell` 表现层 |
| 单元测试 | 重点测 `board`、`reveal`、`victory` |

---

## 9. 实现顺序

1. 初始化 `Vite + React + TypeScript` 项目
2. 建立 `game` 纯逻辑模块和核心类型
3. 实现棋盘创建、首次点击布雷、相邻雷数计算
4. 实现翻开、插旗、胜负判定
5. 搭建 `GameHeader`、`Board`、`Cell` 组件
6. 添加基础样式
7. 启动开发服务器验证页面能打开
8. 手测核心流程：首点安全、踩雷失败、空白展开、标旗、胜利、重开

---

## 10. 待确认 / 待修改项

> 请在这里写下你的想法与修改，后续实现将以此为准。

- [是] 是否坚持 `Vite + React + TypeScript`，还是改用其他技术栈（如原生 HTML/JS、Vue）？
- [否] 初版是否需要**多个预设难度**（初级 / 中级 / 高级），还是只做单一 `9 x 9 / 10 雷`？
- [否] 首次点击是否连 8 邻域一起排除地雷？
- [是] 计时器是否从第一次点击开始计时？
- [否] 是否需要胜利 / 失败弹窗，还是仅用状态栏提示？
- [现代扁平风] 视觉风格倾向：经典 Windows 扫雷复古风 / 现代扁平风 / 其他？
- [是] 是否需要中文本地化（按钮、提示文案）？
- [ ] 其他补充想法：

```text

```
